apiVersion: v1
kind: Service
metadata:
  name: swagger-ui
  namespace: zap-demo
spec:
  selector: { app: swagger-ui }
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP